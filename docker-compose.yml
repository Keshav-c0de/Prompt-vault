services:
  backend:
    build:
      context: ./backend
      dockerfile: backend.Dockerfile

    env_file:
      - ./backend/.env 
    ports:
      - "8000:8000"

  frontend:    
    build:
      context: ./frontend
      dockerfile: frontend.Dockerfile

    ports:
      - "8080:80" 
      